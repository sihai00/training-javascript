/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * 题意：给定一颗二叉树，验证起是否为二分搜索树
 * @param {TreeNode} root
 * @return {boolean}
 */
var isValidBST = function(root) {
  
};